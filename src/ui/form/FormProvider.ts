import { StatelessComponent } from 'react';

interface Props {
   // tslint:disable-next-line:no-any
   model: any;
   // tslint:disable-next-line:no-any
   onSubmit: any;
}

const FormProvider: StatelessComponent<Props> = ({ model, onSubmit }) =>
   null;

export default FormProvider;
